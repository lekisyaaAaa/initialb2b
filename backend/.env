# Environment Configuration
NODE_ENV=development
PORT=5000


# Database Configuration - PostgreSQL for Sequelize
## NOTE: updated to point to Postgres instance on port 5075. Replace credentials as needed.
## Using the postgres password set during the interactive reset session. Replace if different.
## Using the generated postgres password created during reset
## NOTE: password contains special characters; percent-encode them in the URL
DATABASE_URL=postgres://postgres:bN8%23t7QpV4%21s2Z@127.0.0.1:5075/beantobin
# MONGODB_URI=mongodb://localhost:27017/beantobin_environmental_monitoring
# Alternative: Use MongoDB Atlas for cloud database
# MONGODB_URI=mongodb+srv://test_user:test_password@cluster0.mongodb.net/environmental_monitoring?retryWrites=true&w=majority
# DATABASE_URL=postgres://postgres:password@127.0.0.1:5075/beantobin
# NOTE: Older branches referenced MongoDB. MongoDB support is deprecated and
# legacy migration scripts that require Mongo are located in backend/legacy_migrations/.
# If you intentionally need Mongo for migration, set MONGODB_URI in your environment.

# JWT Secret (Generate a strong secret in production)
JWT_SECRET=f7bbcc8b3b70f95aff104e23c7cc39e0ec18978b1d246e4958289c2abc64976b4937759bb4fcc792a2d3b6d62835ff19

# Twilio SMS Configuration (Optional for initial testing)
TWILIO_ACCOUNT_SID=your_twilio_account_sid_here
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
TWILIO_PHONE_NUMBER=your_twilio_phone_number_here

# Alert Thresholds (Default values)
DEFAULT_TEMP_THRESHOLD=30
DEFAULT_HUMIDITY_THRESHOLD=80
DEFAULT_MOISTURE_THRESHOLD=20

# Rate Limiting
API_RATE_LIMIT_WINDOW_MS=900000
API_RATE_LIMIT_MAX_REQUESTS=100

# CORS Origins (comma-separated for multiple origins)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001,http://localhost:3002,http://127.0.0.1:3002

# Local admin fallback disabled to enforce PostgreSQL-only for this project.
ENABLE_LOCAL_ADMIN=false
