// Central suppression flag for all telemetry/data output
export const DATA_SUPPRESSED = true;
export const suppressionReason = 'All telemetry and sensor data is intentionally hidden in this deployment.';
