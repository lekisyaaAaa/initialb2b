# ESPHome Secrets for Knights ESP32
# Copy this file to your ESPHome directory as secrets.yaml

# MQTT Configuration
mqtt_broker_url: "mqtt://your-mqtt-broker.example.com:1883"
mqtt_username: "Knights_IOT"
mqtt_password: "smbcr-5540"

# WiFi Fallback (already configured in main YAML)
# wifi_ssid: "Knights_IOT"
# wifi_password: "smbcr-5540"

# API Password (already configured in main YAML)
# api_password: "Bean2bin2025"

# OTA Password (already configured in main YAML)
# ota_password: "Bean2bin2025"